# 信创数据库生命周期执行器实施计划

## 版本与交付基线

| Flavor | 当前实施基线 | 官方版本或交付来源 | 单机交付要求 |
| --- | --- | --- | --- |
| `oceanbase` | `v4.4.2_CE_BP2` | `https://github.com/oceanbase/oceanbase/releases` | 官方 RPM 与 SHA-256 清单 |
| `gaussdb-mysql` | 厂商当前支持的 MySQL 8.0 私有化版本 | `https://www.huaweicloud.com/intl/en-us/product/gaussdb.html` | 厂商交付的私有化介质与许可证 |
| `polardb-mysql` | 厂商当前支持的 MySQL 8.0 私有化版本 | `https://www.alibabacloud.com/help/polardb/latest/polardb-for-mysql` | 厂商交付的私有化介质与许可证 |
| `tdsql-mysql` | 厂商当前支持的 MySQL/MariaDB 私有云版本 | `https://www.tencentcloud.com/products/tdsql` | 厂商交付的私有云介质与许可证 |
| `tidb` | `v8.5.7` | `https://github.com/pingcap/tidb/releases` | 官方二进制制品与 SHA-256 清单 |
| `kingbase` | `KES V9R1C10` | `https://docs.kingbase.com.cn/cn/KES-V9R1C10/introduction` | 下载中心的授权安装包与校验清单 |
| `opengauss` | `6.0.5` | `https://opengauss.org/en/download/?version=all` | 官方安装包与 SHA-256 清单 |
| `highgo` | `HGDB V9.0` | `https://www.highgo.com/` | 下载中心的授权安装包与校验清单 |
| `gbase8a` | 厂商当前支持版本 | `https://www.gbase.cn/product/gbase-8a` | 厂商交付的安装包、节点组件清单与许可证 |
| `shentong` | 厂商当前支持版本 | `http://shentongdata.com/index.php/download/list-27` | 厂商交付的安装包、校验清单与许可证 |
| `dm` | `DM9` | `https://www.dameng.com/download/index.html` | 下载中心的授权安装包与校验清单 |
| `gbase8s` | 厂商当前支持版本 | `https://www.gbase.cn/product/gbase-8s` | 厂商交付的安装包、校验清单与许可证 |

版本清单必须在每次实施开始前重新核验。商业产品的“当前支持版本”以实施日的授权下载中心或支持门户结果为准，任务记录必须保存该版本、来源 URL、查询日期、安装包文件名和 SHA-256。

- [ ] 1. 建立信创执行器框架与发布门禁
  - [x] 1.1 在每个 flavor 开始实现前，从产品官方发布页获取当前稳定维护/LTS 版本，写入版本清单、下载 URL、发布日期、SHA-256 和查询日期；验收：OceanBase 社区版以 `v4.4.2_CE_BP2`、TiDB 以 `v8.5.7`、openGauss 以 6.0 LTS 作为当前基线，其余商业产品以实施日的厂商支持门户结果为准。
  - [x] 1.2 约定单机部署和本地安装包目录 `/opt/dbops/packages/<flavor>/<version>/`，执行器只读取已放置在目标服务器上的安装包和校验清单；验收：缺少本地包、版本不匹配或 SHA-256 不一致时，部署在执行任何安装命令前失败。
  - [x] 1.3 在 `backend/internal/plugins/kernel` 定义信创数据库核心插件基类，并为安装部署、配置管理、备份恢复、升级迁移、HA/复制、故障切换、监控采集、卸载实现统一的执行器契约；验收：每个执行器都具备 `Prepare`、`Execute`、`Rollback`、`Teardown`、`Join`、`Leave` 路径，并以真实 Agent 任务结果驱动任务状态。
   - [x] 1.4 在 `agent/internal/executor` 按 flavor 注册独立任务处理器、命令构造器和版本探测器；验收：任务载荷必须包含 flavor、版本、目标实例、安装包路径和 TLS 配置，任何 flavor 不匹配的请求返回不可执行错误。
   - [x] 1.5 将 `flavorCapabilities` 改为按完成的执行器逐项启用能力；验收：单机阶段仅可启用部署、配置、备份恢复、升级迁移、监控和卸载能力，`CapReplication`、`CapFailover`、`CapScale`、`CapNodeRebuild` 保持关闭直到多节点真实环境测试通过。
   - [x] 1.6 建立执行器契约测试和真实环境集成测试夹具；验收：每个 flavor 的成功路径、缺工具路径、校验失败路径、权限不足路径、回滚路径和 capability 拒绝路径均可自动验证。

- [ ] 2. 实现 OceanBase 生命周期执行器
   - [x] 2.1 实现 OceanBase 安装部署和配置管理，覆盖 OBServer、OBProxy、租户、资源单元、参数模板及 TLS；验收：在空主机安装指定版本，创建租户并通过 OceanBase 客户端查询版本、节点状态和参数生效结果。真实环境验收需要目标主机预置已校验的 RPM、证书和管理员凭据。
   - [x] 2.2 实现 OceanBase 备份恢复与升级迁移，调用租户级备份、归档、恢复和官方滚动升级流程；验收：全量备份可恢复到新租户，校验 schema、行数和校验和，滚动升级后全部副本恢复 `ACTIVE`。真实环境验收需要可用备份存储、源数据和 OceanBase 集群。
  - [ ] 2.3 实现 OceanBase 单机监控和卸载，并实现多节点 HA/复制/故障切换请求的前置校验；验收：单机指标连续采集，卸载前完成备份并删除本地服务，多节点请求返回明确的 capability 错误。

- [ ] 3. 实现 GaussDB for MySQL 生命周期执行器
  - [ ] 3.1 实现 GaussDB MySQL 私有化单机安装、实例接入、参数配置和 TLS；验收：执行器只接受厂商交付的私有化介质，安装后记录引擎版本并通过引擎查询验证参数生效。
  - [ ] 3.2 实现 GaussDB MySQL 备份恢复与升级迁移，使用产品支持的快照、PITR、逻辑导出和版本升级 API；验收：备份可恢复到隔离实例，数据一致性校验通过，升级前后业务连接和版本状态正确。
  - [ ] 3.3 实现 GaussDB MySQL 单机监控和卸载，并实现主备、只读副本和故障切换请求的前置校验；验收：单机连接、容量和日志指标可见，退役操作完成备份、摘流和本地资源清理，多节点请求返回明确的 capability 错误。

- [ ] 4. 实现 PolarDB for MySQL 生命周期执行器
  - [ ] 4.1 实现 PolarDB MySQL 私有化单机安装、参数模板、账号和 TLS 配置；验收：执行器只接受厂商交付的私有化介质，单机服务可连通，参数持久化且重启后保持一致。
  - [ ] 4.2 实现 PolarDB MySQL 快照备份、时间点恢复、跨集群迁移和小版本/大版本升级；验收：恢复集群的数据校验通过，迁移切换有明确只读窗口和回滚点，升级结果与目标版本一致。
  - [ ] 4.3 实现 PolarDB MySQL 单机监控和卸载，并实现读写切换、故障转移和只读节点请求的前置校验；验收：单机指标进入监控，删除前完成最终备份并清理本地服务与访问配置，多节点请求返回明确的 capability 错误。

- [ ] 5. 实现 TDSQL for MySQL 生命周期执行器
  - [ ] 5.1 实现 TDSQL MySQL 私有云单机部署、参数管理、账号与加密连接；验收：执行器只接受厂商交付的私有云介质，单机组件可连通并验证路由与参数状态。
  - [ ] 5.2 实现 TDSQL MySQL 备份恢复、逻辑迁移和版本升级，采用产品备份服务与支持的迁移工具；验收：恢复后的分片数据、全局一致性和路由查询均正确，升级过程产生可追踪任务日志。
  - [ ] 5.3 实现 TDSQL MySQL 单机监控和卸载，并实现复制、故障切换和扩缩容请求的前置校验；验收：单机服务、容量和日志指标可观测，卸载完成备份、摘流与本地资源清理，多节点请求返回明确的 capability 错误。

- [ ] 6. 实现 TiDB 生命周期执行器
  - [ ] 6.1 实现 TiDB、PD、TiKV 单机共置部署、配置变更、证书和账户管理；验收：部署后 `tiup cluster display` 或等价 API 显示本地组件正常，配置变更按组件完成受控重载。
  - [ ] 6.2 实现 TiDB BR 备份恢复、PITR、Lightning/Dumpling 迁移和滚动升级；验收：BR 恢复和 PITR 后校验数据一致，迁移吞吐和错误记录入任务日志，升级后集群健康检查全绿。
  - [ ] 6.3 实现 TiDB 单机组件监控和卸载，并实现 PD/TiKV/TiFlash 多节点、扩缩容和故障切换请求的前置校验；验收：单机 TiDB、PD、TiKV 进程与指标可观测，卸载按组件依赖顺序完成，多节点请求返回明确的 capability 错误。

- [ ] 7. 实现金仓 Kingbase 生命周期执行器
  - [ ] 7.1 实现 Kingbase 安装部署、实例初始化、参数文件管理、角色账号和 TLS；验收：初始化后的实例可通过 Kingbase/PostgreSQL 协议连接，参数与角色配置可查询验证。
  - [ ] 7.2 实现 Kingbase 物理和逻辑备份恢复、时间点恢复与版本升级迁移；验收：备份可恢复到独立实例，恢复后 schema、权限和数据校验通过，升级失败可回退到可用版本。
  - [ ] 7.3 实现金仓单机监控和安全卸载，并实现流复制、主备切换和备库重建请求的前置校验；验收：连接、WAL、容量和进程指标正确，卸载先完成备份与摘流再停止服务和清理数据目录，多节点请求返回明确的 capability 错误。

- [ ] 8. 实现 openGauss 生命周期执行器
  - [ ] 8.1 实现 openGauss 安装、初始化、GUC 配置、用户权限和 SSL 配置；验收：部署脚本支持指定版本和目录，`gs_ctl` 与 SQL 查询均显示实例正常及参数生效。
  - [ ] 8.2 实现 openGauss 备份恢复、归档恢复、逻辑迁移和滚动升级；验收：全量和增量备份可验证恢复，升级前后对象、权限和数据校验一致。
  - [ ] 8.3 实现 openGauss 单机监控和卸载，并实现主备复制、switchover/failover 和节点重建请求的前置校验；验收：WAL、连接和进程指标持续采集，卸载执行备份、停库和清理，多节点请求返回明确的 capability 错误。

- [ ] 9. 实现 HighGo 生命周期执行器
  - [ ] 9.1 实现 HighGo 安装部署、数据库集群初始化、配置文件、角色及 TLS 管理；验收：新实例通过 HighGo 客户端与 PostgreSQL 驱动可连接，动态和静态参数分别完成重载与重启验证。
  - [ ] 9.2 实现 HighGo 备份恢复、PITR、逻辑迁移和版本升级；验收：物理恢复与逻辑恢复均可通过一致性校验，升级任务保留预检、执行和回滚记录。
  - [ ] 9.3 实现 HighGo 单机监控和卸载，并实现主备流复制、故障转移和扩缩容请求的前置校验；验收：WAL、磁盘和进程指标可观测，卸载删除服务与数据目录，多节点请求返回明确的 capability 错误。

- [ ] 10. 实现 GBase 8a 生命周期执行器
  - [ ] 10.1 实现 GBase 8a 管理与数据组件的单机共置安装、节点配置、账号和 TLS 管理；验收：本地组件完整注册，管理客户端可查询节点状态、版本和配置。
  - [ ] 10.2 实现 GBase 8a 备份恢复、数据导入导出、升级迁移和版本回退；验收：备份恢复后分布式表与数据校验通过，导入导出和升级错误可定位到节点与任务阶段。
  - [ ] 10.3 实现 GBase 8a 单机监控和卸载，并实现数据节点高可用和故障替换请求的前置校验；验收：管理进程、数据进程和容量指标可观测，卸载按本地组件依赖顺序完成，多节点请求返回明确的 capability 错误。

- [ ] 11. 实现神通 Shentong 生命周期执行器
  - [ ] 11.1 实现 Shentong 安装部署、实例创建、配置、权限和安全连接管理；验收：指定版本可自动安装并启动，连接、参数和权限通过官方客户端验证。
  - [ ] 11.2 实现 Shentong 备份恢复、迁移导入导出和升级流程；验收：完整备份和恢复包含系统对象及业务数据，迁移后行数和关键查询结果一致，升级失败能够回滚。
  - [ ] 11.3 实现神通单机监控和卸载，并实现 HA、复制和故障转移请求的前置校验；验收：健康、日志、容量和进程指标进入统一监控，卸载清理服务与本地数据资产，多节点请求返回明确的 capability 错误。

- [ ] 12. 实现达梦 DM 生命周期执行器
  - [ ] 12.1 实现达梦安装、实例初始化、DM 配置参数、用户权限和加密连接；验收：实例创建后使用 DM 客户端完成登录、版本查询和参数验证。
  - [ ] 12.2 实现达梦脱机/联机备份恢复、归档恢复、迁移导入导出和升级；验收：恢复到目标时间点后通过数据与日志一致性校验，升级具备预检、备份和回退流程。
  - [ ] 12.3 实现达梦单机监控和卸载，并实现数据守护、主备切换和故障节点恢复请求的前置校验；验收：归档、容量和进程指标可采集，卸载完成备份、摘流、停服务与清理，多节点请求返回明确的 capability 错误。

- [ ] 13. 实现 GBase 8s 生命周期执行器
  - [ ] 13.1 实现 GBase 8s/Informix 安装部署、初始化、配置、用户和 TLS 管理；验收：实例可由官方工具连接，配置变更按需完成动态加载或受控重启。
  - [ ] 13.2 实现 GBase 8s 备份恢复、日志恢复、迁移导入导出和升级；验收：全量备份、逻辑日志和恢复链路可恢复可验证，升级任务具备失败回滚记录。
  - [ ] 13.3 实现 GBase 8s 单机监控和卸载，并实现 HDR/RSS、复制和故障切换请求的前置校验；验收：日志、会话、存储和进程状态可观测，卸载遵循备份、摘流、停止、清理顺序，多节点请求返回明确的 capability 错误。

- [ ] 14. 实现跨 flavor 验收矩阵与前端能力展示
  - [ ] 14.1 为每个已完成的信创 flavor 建立单机部署、配置、备份恢复、升级迁移、监控和卸载的端到端测试场景，并验证 HA/复制/故障切换请求受控拒绝；验收：测试结果可按 flavor、版本、架构和执行阶段追溯。
  - [ ] 14.2 为每个未完成执行器的 flavor 保持 capability 禁用并在前端展示“仅纳管”边界；验收：前端不展示不可执行动作，后端 API 对绕过请求返回 capability 错误。
  - [ ] 14.3 检查点：确保所有测试通过,如有疑问请询问用户；验收：Go 单元测试、Agent 集成测试、前端能力矩阵测试和真实环境验证报告均通过后再启用对应 capability。
